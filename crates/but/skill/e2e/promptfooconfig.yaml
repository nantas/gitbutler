description: GitButler but skill Codex E2E harness
prompts:
  - "{{task_prompt}}"
providers:
  - id: file://dist/providers/codex-integration.js
    config:
      basePath: .
tests:
  - file://scenarios/independent-branch-choice.yaml
  - file://scenarios/stacked-branch-choice.yaml
